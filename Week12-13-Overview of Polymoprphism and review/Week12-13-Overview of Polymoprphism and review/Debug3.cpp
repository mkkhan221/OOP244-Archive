#include <iostream>
using namespace std;
/*
class  Person
{
    int age;
public:
    Person()
    {
    }

    Person(int age)
    {
        this->age = age;
    }
    
    Person& operator < (Person& p)
    {
        return age < p.age ? p : *this;
    }
    int getAge()
    {
        return age;
    }
};

int main()
{
    Person P1(15);
    Person P2(11);
    Person P3;
    cout<<P3.getAge();
    //if P1 is less than P2
   // P3 = P1 < P2; correct
     if (P1 <P2)
        cout<<P3.getAge()<<endl;
     // cout<<P1.getAge()<<endl; //correct
        
}
*/

